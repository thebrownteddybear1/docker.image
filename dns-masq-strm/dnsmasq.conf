# Basic configuration
port=53
listen-address=127.0.0.1,192.168.50.53

# Alternative to filter-aaaa (tells dnsmasq to ignore IPv6 queries)
address=/::/0

# Rest of your config...
bind-dynamic
domain-needed
# ... (the rest of your original file)
